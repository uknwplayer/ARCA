import test from "node:test";import assert from "node:assert/strict";import {createOperatorIdentityState,transitionOperatorIdentity} from "../src/machine-bridge/operator-identity-state.mjs";
test("identidade só vira operational com prova verificada",()=>{let s=createOperatorIdentityState({operatorId:"a",keyFingerprint:"fp"});s=transitionOperatorIdentity(s,"proof-pending");assert.throws(()=>transitionOperatorIdentity(s,"operational"),/PROOF_REQUIRED/);s=transitionOperatorIdentity(s,"operational",{proofVerified:true});assert.equal(s.state,"operational");});
test("retired é terminal",()=>{let s=createOperatorIdentityState({operatorId:"a",keyFingerprint:"fp",state:"operational"});s=transitionOperatorIdentity(s,"retired");assert.throws(()=>transitionOperatorIdentity(s,"proof-pending"),/TRANSITION_DENIED/);});
test("rotação exige nova fingerprint antes de nova prova",()=>{let s=createOperatorIdentityState({operatorId:"a",keyFingerprint:"old",state:"operational"});s=transitionOperatorIdentity(s,"rotating");assert.throws(()=>transitionOperatorIdentity(s,"proof-pending"),/ROTATION_KEY_REQUIRED/);s=transitionOperatorIdentity(s,"proof-pending",{newKeyFingerprint:"new"});assert.equal(s.keyFingerprint,"new");});
