import test from "node:test";import assert from "node:assert/strict";import {createOperatorPossessionChallengeRegistry} from "../src/machine-bridge/operator-possession-challenge-registry.mjs";
test("challenge é aleatório, bounded e one-time",()=>{let now=new Date("2026-09-18T07:00:00Z");const r=createOperatorPossessionChallengeRegistry({clock:()=>now});const a=r.issue({operatorId:"operator-a"}),b=r.issue({operatorId:"operator-a"});assert.notEqual(a.nonce,b.nonce);assert.equal(r.consume({operatorId:"operator-a",nonce:a.nonce}).nonce,a.nonce);assert.throws(()=>r.consume({operatorId:"operator-a",nonce:a.nonce}),/REPLAY/);assert.equal(r.snapshot().durable,false);});
test("challenge falha fechado para owner errado e expiração",()=>{let now=new Date("2026-09-18T07:00:00Z");const r=createOperatorPossessionChallengeRegistry({clock:()=>now});const c=r.issue({operatorId:"operator-a",ttlMs:1000});assert.throws(()=>r.consume({operatorId:"operator-b",nonce:c.nonce}),/IDENTITY_MISMATCH/);now=new Date("2026-09-18T07:00:02Z");assert.throws(()=>r.consume({operatorId:"operator-a",nonce:c.nonce}),/EXPIRED/);});
