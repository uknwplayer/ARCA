import test from "node:test";import assert from "node:assert/strict";import {createAgentDispatchPolicy,bindSpecializedAgents} from "../../src/machine-bridge/event-agent-dispatch-policy.mjs";
test("case candidate wakes only explicitly routed investigators",()=>{const p=createAgentDispatchPolicy({routes:{"case.candidate":["auditor.copilot","auditor.gpt"]}});assert.deepEqual(p.resolve({type:"case.candidate"}),["auditor.copilot","auditor.gpt"]);assert.deepEqual(p.resolve({type:"ci.completed"}),[])});
test("duplicate agents collapse deterministically",()=>{const p=createAgentDispatchPolicy({routes:{"ci.completed":["steward.tech","steward.tech"]}});assert.deepEqual(p.resolve({type:"ci.completed"}),["steward.tech"])});
test("missing implementation is observable, never silently substituted",()=>{const p=createAgentDispatchPolicy({routes:{"auditor.contradiction":["auditor.copilot"]}});const bind=bindSpecializedAgents({policy:p,agents:{}});assert.deepEqual(bind({type:"auditor.contradiction"}),[{id:"auditor.copilot",status:"unavailable"}])});
test("unknown event route fails closed",()=>{assert.throws(()=>createAgentDispatchPolicy({routes:{"shell.execute":["x"]}}),/ROUTE_INVALID/)});
