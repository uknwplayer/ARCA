import test from "node:test";import assert from "node:assert/strict";import {createCopilotAgentReasoner} from "../../src/machine-bridge/copilot-agent-adapter.mjs";
test("Copilot adapter accepts only ARCA audit tool requests",async()=>{const r=createCopilotAgentReasoner({send:async req=>{assert.equal(req.schema,"arca-copilot-auditor-request-v1");return '{"kind":"tool","name":"audit_get_commit","args":{"repo":"o/r","sha":"abcdef1"}}'}});assert.equal((await r.reason({phase:"independent"})).name,"audit_get_commit")});
test("Copilot adapter blocks mutation tools",async()=>{const r=createCopilotAgentReasoner({send:async()=>'{"kind":"tool","name":"git_push","args":{}}'});await assert.rejects(()=>r.reason({}),/TOOL_DENIED/)});
test("Copilot adapter parses final evidence refs",async()=>{const r=createCopilotAgentReasoner({send:async()=>'{"kind":"final","body":"ok","evidenceRefs":["commit:x"]}'});assert.deepEqual(await r.reason({}),{kind:"final",type:"analysis",body:"ok",evidenceRefs:["commit:x"]})});
