import test from "node:test";import assert from "node:assert/strict";import {createAuditorToolInterface} from "../../src/machine-bridge/auditor-tool-interface.mjs";import {projectAuditorToolsForMcp} from "../../src/machine-bridge/auditor-mcp-projection.mjs";
const gateway={read:async x=>x};
test("strict tool validation rejects malformed and extra args",async()=>{const t=createAuditorToolInterface({gateway,agentId:"a"});await assert.rejects(()=>t.audit_get_pull.invoke({repo:"o/r",number:0}),/INVALID_NUMBER/);await assert.rejects(()=>t.audit_get_commit.invoke({repo:"o/r",sha:"notsha"}),/INVALID_SHA/);await assert.rejects(()=>t.audit_get_file.invoke({repo:"o/r",path:"../secret"}),/INVALID_PATH/);await assert.rejects(()=>t.audit_get_pull.invoke({repo:"o/r",number:1,write:true}),/UNKNOWN_ARG/);});
test("MCP schema marks required and optional fields correctly",()=>{const t=createAuditorToolInterface({gateway,agentId:"a"});const m=projectAuditorToolsForMcp(t).find(x=>x.name==="audit_get_file");assert.deepEqual(m.inputSchema.required,["repo","path"]);assert.equal(m.inputSchema.additionalProperties,false);assert.equal(m.inputSchema.properties.ref.type,"string");});
