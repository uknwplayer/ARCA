import test from "node:test";import assert from "node:assert/strict";import {createProviderHttpInvoker,providerDescriptor} from "../../src/machine-bridge/provider-http-adapter.mjs";
test("known providers include Meta and Grok",()=>{assert.equal(providerDescriptor("grok").requiresConfiguredEndpoint,false);assert.equal(providerDescriptor("meta").requiresConfiguredEndpoint,true);});
test("provider invoker requires HTTPS and never returns credential",async()=>{let call;const invoke=createProviderHttpInvoker({provider:"grok",apiKey:"test-secret",fetchImpl:async(u,o)=>(call={u,o},{ok:true,json:async()=>({x:1})}),encodeRequest:r=>r,decodeResponse:()=>({kind:"final",body:"ok"})});assert.deepEqual(await invoke({a:1}),{kind:"final",body:"ok"});assert.equal(call.o.method,"POST");assert.equal(JSON.stringify(await invoke({a:2})).includes("test-secret"),false);assert.throws(()=>createProviderHttpInvoker({provider:"grok",apiKey:"x",endpoint:"http://example.com",fetchImpl:()=>{},encodeRequest:x=>x,decodeResponse:x=>x}),/HTTPS_REQUIRED/);});
test("Meta endpoint is explicit rather than guessed",()=>{assert.throws(()=>createProviderHttpInvoker({provider:"meta",apiKey:"x",fetchImpl:()=>{},encodeRequest:x=>x,decodeResponse:x=>x}),/ENDPOINT_REQUIRED/);});
