export function projectAuditorToolsForMcp(tools){return Object.values(tools).map(t=>{const properties={},required=[];for(const [k,s] of Object.entries(t.inputSchema)){properties[k]={type:s.type};if(!s.optional)required.push(k)}return Object.freeze({name:t.name,description:"ARCA read-only audit operation. No mutation authority.",inputSchema:{type:"object",properties,required,additionalProperties:false},annotations:{readOnlyHint:true,destructiveHint:false,idempotentHint:true,openWorldHint:false},call:args=>t.invoke(args)})})}
