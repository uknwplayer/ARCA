export function createCopilotSdkHost({createClient,systemMessage,maxResponseBytes=128000}={}){if(typeof createClient!=="function")throw new Error("ARCA_COPILOT_CLIENT_FACTORY_REQUIRED");return Object.freeze({async send(request,{signal}={}){if(signal?.aborted)throw new Error("ARCA_COPILOT_ABORTED");const client=await createClient();let session;try{session=await client.createSession({systemMessage:systemMessage??request.instruction});const result=await session.sendAndWait({prompt:JSON.stringify(request),...(signal?{signal}:{})});const text=typeof result==="string"?result:result?.data?.content??result?.content;if(typeof text!=="string")throw new Error("ARCA_COPILOT_EMPTY_RESPONSE");if(Buffer.byteLength(text,"utf8")>maxResponseBytes)throw new Error("ARCA_COPILOT_RESPONSE_TOO_LARGE");return text}finally{if(session?.destroy)await session.destroy();if(client?.stop)await client.stop()}}}})}
